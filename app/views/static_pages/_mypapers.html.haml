.container
  %h1 My recent papers
  %ul
    - @papers.each do |p|
      %li
        = link_to p.title, user_paper_path(p.user.id, p.id)
        %br
        %span= p.description
        %br
        %br
  = link_to "New paper", new_user_paper_path(current_user.id)

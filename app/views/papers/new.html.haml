= form_for [@user, @paper] do |f|
  .field
    = f.label:title
    = f.text_field:title, autofocus:true, autocomplete:"on"

  .field
    = f.label:description
    = f.text_field:description, autofocus:true, autocomplete:"on"

  .field
    = f.label :article
    = f.file_field :article

  .actions
    = f.submit
